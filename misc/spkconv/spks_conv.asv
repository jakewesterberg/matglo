function cnv = spks_conv( cnv, k )
parfor i = 1 : height(cnv)
    cnv(i, :) = paren( conv( [ (mean(cnv(i,1:hlen))*ones(1,hlen)) ...cnv(i,:) ...
        (mean(cnv(i,llen-hlen:llen))*ones(1,hlen)) ] ...
        , k), ...
        round(length(k)-1):round(length(k)-1)+length(cnv)-1);
end
end
